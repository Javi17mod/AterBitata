import { aterbot }  from "aterbot";

const bot = aterbot();

            // Obtén el elemento donde se mostrará el output
            const outputElement = "<h1>On</h1>;

            // Renderiza aterbot en el elemento de salida
            bot.render(outputElement);
